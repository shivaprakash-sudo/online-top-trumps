<%- include ("../partials/after_login_header.ejs") %>

    <!-- <div class="card-details">
    <div>
        <img width="200" height="150" class="card-image" src="<%#= card.cardImagePath %>" />
        <h3 class="page-header">
            <%#= card.cardName %>
        </h3>
        <div class="card-details-btn-grid">
            <a class="btn btn-primary" href="/cards/<%#= card.id %>/edit">Edit</a>
            <%#- include('../partials/deleteForm', { url: `/cards/${card.id}` }) %>
                 <a class="btn btn-primary card-details-user-button" href="/users/<%#= card.addedBy.id %>">View the creator</a>
         </div>
    </div>
    <div class="card-details-grid">
        <div class="card-details-label">Added by:</div>
        <div>
            <%#= card.addedBy.name %>
        </div>
        <div class="card-details-label">Created on</div>
        <div>
            <%#= card.createdAt.toDateString() %>
        </div>
    </div>
</div> -->

    <!-- bootstrap card -->
    <div class="container-fluid card-container" style="max-width: 18rem">
        <div class="card border-light">
            <img width="250" height="175" src="<%= card.cardImagePath %>" class="card-img-top" alt="Image" />
            <div class="card-body">
                <h5 class="card-title">
                    <%= card.cardName %>
                </h5>
                <table class="card-table table border-light">
                    <tbody>
                        <tr>
                            <td>
                                <%= card.attributes[0] %>
                            </td>
                            <td>
                                <%= card.values[0] %>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                <%= card.attributes[1] %>
                            </td>
                            <td>
                                <%= card.values[1] %>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                <%= card.attributes[2] %>
                            </td>
                            <td>
                                <%= card.values[2] %>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <!-- <div class="row">
                    <button class="btn btn-light col">
          <a class="card-link" href="/cards/<%= card.id %>/edit">Edit</a>
        </button>
                    <%- include('../partials/deleteForm', { url: `/cards/${card.id}` }) %>
                </div> -->
            </div>
            <div class="card-footer text-muted small">
                Added by:
                <%= card.addedBy.name %>
            </div>
        </div>
    </div>