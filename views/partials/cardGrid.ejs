<div class="container-fluid">
    <div class="row">
        <% cards.forEach(card => { %>
            <div class="col p-3">
                <div class="card">
                    <img width="250" height="175" class="card-img-top card-image" src="<%= card.cardImagePath %>" />
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= card.cardName %>
                        </h5>
                        <a class="card-link link-primary" href="/cards/<%= card.id %>">
            Click to see the card
          </a>
                    </div>
                </div>
            </div>
            <% }) %>
    </div>
</div>